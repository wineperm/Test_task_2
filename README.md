# Тестовое задание:

Есть несколько (N) nginx в режиме обратного прокси (proxy_pass...). За ними приложение, отвечающее по HTTP. Запрос от пользователя может прийти на любой из nginx! Запрос от пользователя может пройти как через один nginх, так и по цепочке через несколько nginx. Примеры:

пользователь>nginx1>приложение пользователь>nginx2>приложение ... пользователь>nginxN>приложение пользователь>nginx1>nginx2>nginxN>приложение пользователь>nginx2>nginxN>приложение

Приложение проверяет заголовок X-Forwarded-For. Требуется, чтобы приложение в заголовке получило IP адрес пользователя и всех nginx (всю цепочку), через которые прошел запрос. Требуется, чтобы приложение не получило заголовок X-Forwarded-For, который недобросовестный пользователь может добавить в свой запрос.

Решение необходимо подготовить в виде тестового стенда для запуска в docker-compose, минимум из трех серверов nginx и любого готового или своего приложения, выводящего заголовок X-Forwarded-For.

Предоставьте протокол тестирования на выполнение всех требований задания с помощью утилиты curl.

---

### 📌 Nginx Proxy Chain with Secure X-Forwarded-For Handling

**GitHub Repository Description**

---

#### 🎯 **Цель проекта**

Реализация тестового стенда с несколькими Nginx в режиме обратного прокси и приложением, которое корректно обрабатывает заголовок `X-Forwarded-For`:

- **Получает полную цепочку IP-адресов** (пользователь → Nginx1 → Nginx2 → Nginx3 → приложение).
- **Игнорирует поддельные заголовки `X-Forwarded-For`**, отправленные пользователем.

---

#### 🧱 **Архитектура стенда**

- **3 Nginx-сервера** (`nginx1`, `nginx2`, `nginx3`) в режиме обратного прокси.
- **Flask-приложение**, выводящее заголовок `X-Forwarded-For`.
- Все компоненты объединены в одну Docker-сеть для тестирования цепочки запросов.

---

#### 🔐 **Особенности реализации**

1. **Безопасность**:

   - Первый Nginx (`nginx1`) игнорирует входящие заголовки `X-Forwarded-For` и начинает цепочку с реального IP клиента (`$remote_addr`).
   - Последующие Nginx добавляют свои IP в цепочку через `$proxy_add_x_forwarded_for`.

2. **Корректность**:
   - Приложение получает **всю цепочку IP-адресов**, даже если запрос проходит через несколько прокси.
   - Поддельные заголовки `X-Forwarded-For` от пользователя **не попадают в цепочку**.

---

#### 📁 **Структура проекта**

```
project/
├── docker-compose.yml       # Оркестрация контейнеров
├── nginx1.conf              # Конфиг Nginx1 (первый прокси)
├── nginx2.conf              # Конфиг Nginx2 (второй прокси)
├── nginx3.conf              # Конфиг Nginx3 (третий прокси)
└── app/
    ├── Dockerfile           # Сборка Flask-приложения
    └── app.py               # Код приложения (вывод X-Forwarded-For)
```

---

#### 🚀 **Как запустить**

1. Установите Docker и Docker Compose.
2. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/yourusername/nginx-proxy-chain.git
   cd nginx-proxy-chain
   ```
3. Запустите стенд:
   ```bash
   docker compose up -d --build
   ```

---

#### 🧪 **Результаты тестирования**

| Сценарий                            | Команда                                                    | Ответ `X-Forwarded-For`              | Пояснение                                               |
| ----------------------------------- | ---------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------- |
| 1. Прямой запрос через все Nginx    | `curl http://localhost:8080`                               | `172.18.0.1, 172.18.0.3, 172.18.0.5` | Цепочка: клиент → nginx1 → nginx2 → nginx3 → приложение |
| 2. Запрос через `nginx2` и `nginx3` | `curl http://localhost:8081`                               | `172.18.0.1, 172.18.0.5`             | Цепочка: клиент → nginx2 → nginx3 → приложение          |
| 3. Запрос напрямую к `nginx3`       | `curl http://localhost:8082`                               | `172.18.0.1`                         | Цепочка: клиент → nginx3 → приложение                   |
| 4. Поддельный `X-Forwarded-For`     | `curl -H "X-Forwarded-For: 9.9.9.9" http://localhost:8080` | `172.18.0.1, 172.18.0.3, 172.18.0.5` | Подделка игнорируется                                   |

---

#### 📋 **Конфигурации Nginx**

- **`nginx1.conf`** (первый прокси):
  ```nginx
  location / {
      proxy_pass http://nginx2;
      proxy_set_header X-Forwarded-For $remote_addr;  # Используем только IP клиента
  }
  ```
- **`nginx2.conf` и `nginx3.conf`** (промежуточные прокси):
  ```nginx
  location / {
      proxy_pass http://nginx3;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;  # Добавляем свой IP в цепочку
  }
  ```

---

#### 📌 **Почему это работает**

- `$remote_addr` — реальный IP клиента, безопасный источник.
- `$proxy_add_x_forwarded_for` — добавляет IP текущего прокси к цепочке.
- Порядок директив в Nginx важен: сначала очистка заголовка, затем добавление реального IP.

---

#### 📚 **Дополнительные материалы**

- [Официальная документация Nginx](https://nginx.org/ru/docs/)
- [Docker Compose Reference](https://docs.docker.com/compose/reference/)

---

#### ✅ **Заключение**

Этот стенд демонстрирует корректную обработку заголовка `X-Forwarded-For` в цепочке прокси с защитой от подделок. Подходит для тестирования сценариев с множественными уровнями обратного прокси.
